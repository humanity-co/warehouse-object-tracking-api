apiVersion: apps/v1
kind: Deployment
metadata:
  name: warehouse-schema-registry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: warehouse-schema-registry
  template:
    metadata:
      labels:
        app: warehouse-schema-registry
    spec:
      containers:
        - name: schema-registry
          image: confluentinc/cp-schema-registry:7.7.1
          ports:
            - containerPort: 8081
          env:
            - name: SCHEMA_REGISTRY_HOST_NAME
              value: warehouse-schema-registry
            - name: SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS
              value: PLAINTEXT://warehouse-kafka:9092
            - name: SCHEMA_REGISTRY_LISTENERS
              value: http://0.0.0.0:8081
---
apiVersion: v1
kind: Service
metadata:
  name: warehouse-schema-registry
spec:
  selector:
    app: warehouse-schema-registry
  ports:
    - port: 8081
      targetPort: 8081

